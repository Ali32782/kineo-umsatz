# Backend
SECRET_KEY=change-me-to-a-long-random-string
DATA_DIR=./data
EXPORTS_DIR=./exports
ENV=production

# E-Mail (optional — ohne Key werden Mails nur geloggt)
RESEND_API_KEY=
FROM_EMAIL=kineo@kineo.swiss
CEO_EMAILS=ali.peters@kineo.swiss,sereina.urech@kineo.swiss

# Frontend build (nur für lokales npm run dev / Render static build)
# Leer lassen bei Docker (nginx proxied /api)
VITE_API_URL=http://localhost:8000
